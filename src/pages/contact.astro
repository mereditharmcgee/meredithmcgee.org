---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={`Contact | ${SITE_TITLE}`} description="Get in touch with Meredith McGee. I'm always open to new collaborations, opportunities, and conversations about public health, writing, and community impact." />
	</head>
	<body>
		<Header />

		<!-- Hero Section -->
		<section class="contact-hero bg-dark bg-pattern">
			<div class="container">
				<h1>Let's Connect</h1>
				<p class="hero-intro">
					I'm always interested in new opportunities to collaborate, whether that means working on a project together, exploring a potential role, or simply having a conversation about public health, writing, or community-centered work. If something on this site resonated with you, or if you think there might be a way we could work together, I'd love to hear from you.
				</p>
			</div>
		</section>

		<!-- Contact Content -->
		<section class="contact-content bg-light">
			<div class="container">
				<div class="contact-grid">
					<div class="contact-info">
						<h2>Get in Touch</h2>
						<p>
							Whether you're interested in discussing a potential collaboration, have a question about my work, or want to explore how we might work together, I welcome the conversation.
						</p>
						<p>
							I'm particularly interested in opportunities involving:
						</p>
						<ul class="interest-list">
							<li>Grant writing and development</li>
							<li>Program evaluation and design</li>
							<li>Public health research and writing</li>
							<li>Community-based projects</li>
							<li>LGBTQ+ health initiatives</li>
						</ul>

						<div class="direct-contact">
							<h3>Prefer email?</h3>
							<p>You can reach me directly at:</p>
							<a href="mailto:meredith.ar.mcgee@gmail.com" class="email-link">meredith.ar.mcgee@gmail.com</a>
						</div>
					</div>

					<div class="contact-form-wrapper">
						<form action="https://formspree.io/f/YOUR_FORM_ID" method="POST" class="contact-form">
							<div class="form-group">
								<label for="name">Name</label>
								<input type="text" id="name" name="name" required />
							</div>

							<div class="form-group">
								<label for="email">Email</label>
								<input type="email" id="email" name="email" required />
							</div>

							<div class="form-group">
								<label for="subject">Subject</label>
								<select id="subject" name="subject" required>
									<option value="">Select a topic...</option>
									<option value="collaboration">Potential Collaboration</option>
									<option value="opportunity">Job/Contract Opportunity</option>
									<option value="question">Question About My Work</option>
									<option value="speaking">Speaking/Event Inquiry</option>
									<option value="other">Other</option>
								</select>
							</div>

							<div class="form-group">
								<label for="message">Message</label>
								<textarea id="message" name="message" rows="5" required placeholder="Tell me a bit about what you have in mind..."></textarea>
							</div>

							<button type="submit" class="btn submit-btn">Send Message</button>
						</form>
					</div>
				</div>
			</div>
		</section>

		<Footer />
	</body>
</html>

<style>
	/* Hero Section */
	.contact-hero {
		padding-top: 5rem;
		padding-bottom: 2.5rem;
		text-align: center;
	}

	.contact-hero .container {
		max-width: 800px;
		padding: 0 2rem;
	}

	.contact-hero h1 {
		font-size: clamp(2rem, 4vw, 3rem);
		margin-bottom: 1rem;
	}

	.hero-intro {
		font-size: clamp(0.9rem, 1.1vw, 1.05rem);
		line-height: 1.8;
		opacity: 0.95;
	}

	/* Contact Content */
	.contact-content {
		padding: 3rem 0;
	}

	.contact-content .container {
		max-width: 1100px;
		padding: 0 2rem;
	}

	.contact-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3rem;
		align-items: start;
	}

	.contact-info h2 {
		font-size: clamp(1.5rem, 2.5vw, 2rem);
		color: var(--color-dark);
		margin-bottom: 1rem;
	}

	.contact-info p {
		font-size: clamp(0.85rem, 1vw, 0.95rem);
		line-height: 1.7;
		color: var(--color-dark);
		margin-bottom: 1rem;
	}

	.interest-list {
		list-style: none;
		padding: 0;
		margin: 0 0 1.5rem 0;
	}

	.interest-list li {
		font-size: clamp(0.85rem, 1vw, 0.95rem);
		color: var(--color-dark);
		padding: 0.35rem 0;
		padding-left: 1.25rem;
		position: relative;
	}

	.interest-list li::before {
		content: '';
		position: absolute;
		left: 0;
		top: 50%;
		transform: translateY(-50%);
		width: 6px;
		height: 6px;
		background: var(--color-medium);
		border-radius: 50%;
	}

	.direct-contact {
		margin-top: 2rem;
		padding-top: 1.5rem;
		border-top: 1px solid rgba(61, 74, 42, 0.2);
	}

	.direct-contact h3 {
		font-size: clamp(1.1rem, 1.5vw, 1.25rem);
		color: var(--color-dark);
		margin-bottom: 0.5rem;
	}

	.direct-contact p {
		margin-bottom: 0.5rem;
	}

	.email-link {
		font-size: clamp(0.95rem, 1.2vw, 1.1rem);
		color: var(--color-medium);
		font-weight: 600;
		text-decoration: none;
		transition: color 0.3s ease;
	}

	.email-link:hover {
		color: var(--color-dark);
		text-decoration: underline;
	}

	/* Contact Form */
	.contact-form-wrapper {
		background: var(--color-dark);
		padding: 2rem;
		border-radius: 12px;
		box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
	}

	.contact-form {
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
	}

	.form-group {
		display: flex;
		flex-direction: column;
		gap: 0.35rem;
	}

	.form-group label {
		font-size: 0.85rem;
		font-weight: 500;
		color: var(--color-gold);
	}

	.form-group input,
	.form-group select,
	.form-group textarea {
		padding: 0.75rem;
		border: 1px solid rgba(212, 201, 142, 0.3);
		border-radius: 6px;
		background: rgba(255, 255, 255, 0.05);
		color: var(--color-cream);
		font-family: 'Source Sans 3', sans-serif;
		font-size: 0.9rem;
		transition: border-color 0.3s ease, background 0.3s ease;
	}

	.form-group input:focus,
	.form-group select:focus,
	.form-group textarea:focus {
		outline: none;
		border-color: var(--color-gold);
		background: rgba(255, 255, 255, 0.08);
	}

	.form-group input::placeholder,
	.form-group textarea::placeholder {
		color: rgba(232, 228, 208, 0.5);
	}

	.form-group select {
		cursor: pointer;
	}

	.form-group select option {
		background: var(--color-dark);
		color: var(--color-cream);
	}

	.form-group textarea {
		resize: vertical;
		min-height: 120px;
	}

	.submit-btn {
		margin-top: 0.5rem;
		background: var(--color-gold);
		color: var(--color-dark);
		border: 2px solid var(--color-gold);
		font-weight: 600;
		cursor: pointer;
	}

	.submit-btn:hover {
		background: transparent;
		color: var(--color-gold);
	}

	/* Responsive */
	@media (max-width: 768px) {
		.contact-hero {
			padding-top: 4rem;
		}

		.contact-grid {
			grid-template-columns: 1fr;
			gap: 2rem;
		}

		.contact-info {
			text-align: center;
		}

		.interest-list li {
			text-align: left;
		}

		.contact-form-wrapper {
			padding: 1.5rem;
		}
	}
</style>
