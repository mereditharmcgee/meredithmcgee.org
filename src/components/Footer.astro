---
const currentYear = new Date().getFullYear();
---

<footer>
	<div class="container">
		<div class="footer-content">
			<div class="footer-info">
				<h3>Meredith McGee, MPH</h3>
				<p class="footer-subtitle">Public health writer, evaluator, and researcher</p>
				<p class="footer-tagline">Connecting community needs and data through writing.</p>
			</div>
			<div class="footer-contact">
				<a href="/contact" class="btn">Get in Touch!</a>
				<p class="footer-email">
					<a href="mailto:meredith.ar.mcgee@gmail.com">meredith.ar.mcgee@gmail.com</a>
				</p>
				<p class="footer-location">Boston, Massachusetts</p>
			</div>
		</div>
		<div class="footer-bottom">
			<p>&copy; {currentYear} Meredith McGee. All rights reserved.</p>
		</div>
	</div>
</footer>

<style>
	footer {
		padding: 6rem 0 3rem;
		background: var(--color-dark);
	}

	footer .container {
		max-width: 100%;
		padding: 0 4rem;
	}

	.footer-content {
		display: flex;
		justify-content: space-between;
		align-items: flex-start;
		gap: 4rem;
		padding-bottom: 3rem;
		border-bottom: 1px solid rgba(212, 201, 142, 0.3);
	}

	.footer-info h3 {
		font-family: 'Playfair Display', Georgia, serif;
		color: var(--color-gold);
		font-size: 3.5rem;
		margin-bottom: 1rem;
	}

	.footer-subtitle {
		color: var(--color-cream);
		font-size: 2rem;
		margin-bottom: 0.5rem;
	}

	.footer-tagline {
		color: var(--color-cream);
		font-style: italic;
		font-size: 1.8rem;
		opacity: 0.9;
	}

	.footer-contact {
		text-align: right;
	}

	.footer-contact .btn {
		margin-bottom: 1.5rem;
		border-color: var(--color-gold);
		color: var(--color-gold);
		font-size: 1.8rem;
		padding: 1rem 2.5rem;
	}

	.footer-contact .btn:hover {
		background: var(--color-gold);
		color: var(--color-dark);
	}

	.footer-email a {
		color: var(--color-cream);
		text-decoration: none;
		font-size: 1.8rem;
	}

	.footer-email a:hover {
		text-decoration: underline;
		color: var(--color-gold);
	}

	.footer-location {
		color: var(--color-cream);
		font-size: 1.6rem;
		opacity: 0.9;
	}

	.footer-bottom {
		padding-top: 2rem;
		text-align: center;
	}

	.footer-bottom p {
		color: var(--color-cream);
		font-size: 1.4rem;
		opacity: 0.8;
		margin: 0;
	}

	@media (max-width: 768px) {
		footer .container {
			padding: 0 2rem;
		}

		.footer-content {
			flex-direction: column;
			text-align: center;
			gap: 2rem;
		}

		.footer-contact {
			text-align: center;
		}

		.footer-info h3 {
			font-size: 2.5rem;
		}

		.footer-subtitle {
			font-size: 1.5rem;
		}

		.footer-tagline {
			font-size: 1.4rem;
		}
	}
</style>
