# External Integrations

**Analysis Date:** 2025-02-28

## APIs & External Services

**Form Handling:**
- Formspree - Email form backend service
  - SDK/Client: Web form submission (direct HTML form POST)
  - Endpoint: `https://formspree.io/f/xgooeagz`
  - Implementation: `src/pages/contact.astro` line 54
  - Fields: name, email, subject, message
  - Method: POST to Formspree endpoint (processes form and sends email)

## Data Storage

**Databases:**
- None - fully static site with no server-side database

**File Storage:**
- Local filesystem in `public/` directory
  - Images: `public/hero-image.jpg` and other assets
  - Fonts: `public/fonts/` (Atkinson Regular/Bold woff files)
  - Icons: `logo-m-new.png`
  - Site: `public/sitemap-index.xml` (auto-generated)

**Static Asset Serving:**
- GitHub Pages serves all files from `dist/` after build
- Assets referenced with root paths: `/hero-image.jpg`, `/logo-m-new.png`, `/fonts/...`

**Caching:**
- None explicitly configured - relies on HTTP caching headers from GitHub Pages

## Content Management

**Content Sources:**
- Markdown files in `src/content/blog/` - Blog posts with frontmatter metadata
- Markdown files in `src/content/portfolio/` - Portfolio project entries with extended metadata
- Static pages in `src/pages/` - Individual Astro components for about, work, resume, contact

## Authentication & Identity

**Auth Provider:**
- None - fully public static site, no user authentication

## Monitoring & Observability

**Error Tracking:**
- None detected

**Logs:**
- GitHub Actions workflow logs visible in repository Actions tab
- Build output captured in CI/CD pipeline

**Analytics:**
- None detected in codebase

## CI/CD & Deployment

**Hosting:**
- GitHub Pages - Automatic deployment from Actions workflow
- Repository: Remote git repository (origin)
- Branch: `main` is production branch

**CI Pipeline:**
- GitHub Actions (`.github/workflows/deploy.yml`)
- Trigger: Push to main or manual workflow_dispatch
- Steps:
  1. Checkout code
  2. Setup Node 20 with npm cache
  3. `npm ci` - Clean install dependencies
  4. `npm run build` - Build Astro site to `dist/`
  5. Upload artifact to GitHub Pages
  6. Deploy Pages artifact

**Build Outputs:**
- Static HTML site in `dist/` directory
- Sitemaps: Auto-generated `dist/sitemap-index.xml`
- RSS feed: Auto-generated (referenced in `src/components/BaseHead.astro` line 28-30)

## External CDNs & Resources

**Google Fonts:**
- Source: `https://fonts.googleapis.com/css2`
- Fonts: Playfair Display (serif headings), Source Sans 3 (body text)
- Integration: Imported in `src/styles/global.css` line 1
- Scope: Applied globally via CSS custom font-family declarations

## Environment Configuration

**Required env vars:**
- None detected - fully static site requires no runtime environment variables

**Secrets location:**
- No secrets management needed

**Configuration:**
- Site URL: Hardcoded in `astro.config.mjs` as `'https://meredithmcgee.org'`
- Content paths: Convention-based (Astro auto-discovers `src/content/` collections)
- Build output: Configured to `dist/` (Astro default)

## Webhooks & Callbacks

**Incoming:**
- Formspree form submission callback: Email sent from `meredith.ar.mcgee@gmail.com` when contact form submitted

**Outgoing:**
- None

## Static Resource Delivery

**RSS Feed:**
- Endpoint: `/rss.xml` (auto-generated by @astrojs/rss)
- Referenced in BaseHead as alternate link for RSS readers
- Generated at build time

**Sitemap:**
- Endpoint: `/sitemap-index.xml` (auto-generated by @astrojs/sitemap)
- Referenced in BaseHead for search engine crawlers
- Generated at build time

## Third-Party Scripts

**None detected** - No external JavaScript tracking, analytics, or widget libraries

---

*Integration audit: 2025-02-28*
