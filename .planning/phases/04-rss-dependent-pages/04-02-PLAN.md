---
phase: 04-rss-dependent-pages
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/pages/ground-level.astro
autonomous: false
requirements:
  - GRLV-01
  - GRLV-02
  - GRLV-03

must_haves:
  truths:
    - "The Ground Level page displays a brief description of what Ground Level is"
    - "The Ground Level page has a prominent 'Subscribe on Substack' button linking to the Substack URL"
    - "The Ground Level page shows 6 recent Substack posts with title, date, excerpt, and link"
    - "Both the homepage and Ground Level page look correct when viewed in a browser"
  artifacts:
    - path: "src/pages/ground-level.astro"
      provides: "Complete Ground Level page with description, subscribe CTA, and post feed"
      min_lines: 50
      contains: "SubstackFeed"
  key_links:
    - from: "src/pages/ground-level.astro"
      to: "src/components/SubstackFeed.astro"
      via: "import and render with limit={6}"
      pattern: "SubstackFeed.*limit=.6."
    - from: "src/pages/ground-level.astro"
      to: "src/consts.ts"
      via: "import SUBSTACK_URL for subscribe CTA"
      pattern: "SUBSTACK_URL"
    - from: "src/pages/ground-level.astro"
      to: "https://meredithmcgee.substack.com"
      via: "Subscribe button href"
      pattern: "Subscribe on Substack"
---

<objective>
Rewrite the Ground Level page (ground-level.astro) from its current stub into a full page with an intro description, Substack subscribe CTA, and 6 recent posts via SubstackFeed. Then verify both the homepage and Ground Level visually.

Purpose: Ground Level is the bridge between the site and Meredith's Substack. The current stub has no SubstackFeed integration, no styled CTA, and no section structure. This plan completes it and verifies the entire phase visually.

Output: A complete `src/pages/ground-level.astro` plus user visual sign-off on both Phase 4 pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-rss-dependent-pages/04-CONTEXT.md
@.planning/phases/04-rss-dependent-pages/04-RESEARCH.md
@.planning/phases/04-rss-dependent-pages/04-01-SUMMARY.md

Source files to read before implementing:
@src/pages/ground-level.astro (current stub — full rewrite)
@src/pages/work.astro (structural pattern to follow)
@src/components/SubstackFeed.astro (component interface)
@src/consts.ts (SITE_TITLE, SUBSTACK_URL)
@src/styles/global.css (bg-dark, bg-pattern, .btn, .container)

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/SubstackFeed.astro:
```typescript
interface Props {
  limit?: number;  // defaults to 3 if omitted
}
// Usage: <SubstackFeed limit={6} />
// Renders post cards with time, h3+a, p — uses gold/cream tokens (MUST be on dark bg)
```

From src/consts.ts:
```typescript
export const SITE_TITLE = 'Meredith McGee, MPH';
export const SUBSTACK_URL = 'https://meredithmcgee.substack.com';
```

Subscribe CTA pattern (from RESEARCH.md):
```astro
<a href={SUBSTACK_URL} target="_blank" rel="noopener noreferrer" class="btn">
  Subscribe on Substack
</a>
```
The .btn class is defined in global.css — gold outline, hover fills gold with dark text.

Page structure pattern (no layout wrapper, standalone file):
- Imports: BaseHead, Footer, Header, then page-specific components
- Alternating section backgrounds
- Scoped <style> at the end
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ground-level.astro with SubstackFeed and subscribe CTA</name>
  <files>src/pages/ground-level.astro</files>
  <action>
  DELETE the entire contents of src/pages/ground-level.astro and rewrite from scratch. The current stub has no SubstackFeed, no sections, and no styled CTA. This is a full rewrite.

  **Frontmatter block:**
  - Import BaseHead, Footer, Header, SubstackFeed
  - Import SITE_TITLE, SUBSTACK_URL from '../consts'

  **HTML structure — two sections between Header and Footer:**

  **Section 1: Intro + CTA** — `<section class="gl-intro bg-dark bg-pattern">`
  - Container max-width: 800px
  - `<h1>Ground Level</h1>` — centered
  - Description paragraph: `<p class="gl-description">Ground Level is my Substack about cannabis, public health, and the gap between policy and practice.</p>` — This is the locked copy from CONTEXT.md. Do not edit.
  - Subscribe CTA button directly below the description, centered:
    ```
    <div class="gl-cta">
      <a href={SUBSTACK_URL} target="_blank" rel="noopener noreferrer" class="btn">
        Subscribe on Substack
      </a>
    </div>
    ```
  - Padding-top: 6rem (below fixed header), padding-bottom: 3rem

  **Section 2: Post Feed** — `<section class="gl-posts bg-dark">`
  - Container max-width: 800px
  - `<h2>Recent posts</h2>` — centered, casual
  - `<SubstackFeed limit={6} />` — shows up to 6 posts
  - Standard section padding

  **Scoped styles block:**
  - `.gl-intro`: padding-top 6rem, text-align center
  - `.gl-intro .container`: max-width 800px, padding 0 2rem
  - `.gl-intro h1`: font-size clamp(2rem, 4vw, 3rem), margin-bottom 1.5rem
  - `.gl-description`: font-size clamp(0.95rem, 1.1vw, 1.1rem), line-height 1.8, max-width 600px, margin 0 auto, opacity 0.95
  - `.gl-cta`: margin-top 2rem (space between description and button)
  - `.gl-posts .container`: max-width 800px, padding 0 2rem
  - `.gl-posts h2`: font-size clamp(1.5rem, 2.5vw, 2rem), margin-bottom 1.5rem, text-align center
  - Mobile breakpoint (@media max-width 768px): .gl-intro padding-top 5rem

  **Anti-patterns to avoid:**
  - Do NOT keep the old `<main class="ground-level">` wrapper — use `<section>` elements (matches work.astro pattern)
  - Do NOT put SubstackFeed on a bg-light section — it uses gold/cream tokens that need a dark background
  - Do NOT use an embedded Substack form — CTA is a plain link styled as .btn per locked decision
  - Do NOT hardcode the Substack URL — import SUBSTACK_URL from consts.ts
  - Do NOT edit the description copy — it is locked in CONTEXT.md
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - Build succeeds with zero errors
  - ground-level.astro contains the locked description paragraph verbatim
  - ground-level.astro has a .btn link to SUBSTACK_URL with "Subscribe on Substack"
  - ground-level.astro includes SubstackFeed with limit={6}
  - No old stub markup remains (no `<main class="ground-level">`)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of homepage and Ground Level page</name>
  <files>src/pages/index.astro, src/pages/ground-level.astro</files>
  <action>
  Start the dev server (`npm run dev`) and present both pages for user review.

  Both RSS-dependent pages are now complete:
  1. **Homepage** (/) — three-beat layout: Intro with name + photo + copy, What I'm Working On with 3 Substack posts, Selected Work with 4 work items + "See all work" link
  2. **Ground Level** (/ground-level) — intro description, subscribe CTA button, 6 recent Substack posts

  **Homepage verification steps (http://localhost:4321/):**
  1. The page opens with "Meredith McGee" as the heading (no "MPH")
  2. The headshot photo is visible and centered below the name
  3. The intro paragraph reads naturally and matches the final copy
  4. Scrolling down, "What I'm working on" shows 3 Substack posts with titles, dates, and excerpts
  5. Further down, "Selected work" shows 4 work items with category tags and descriptions
  6. A "See all work" link appears below the work items and links to /work
  7. Overall feel: clean, personal, browsable — "person first" tone

  **Ground Level verification steps (http://localhost:4321/ground-level):**
  1. The page shows "Ground Level" as the heading
  2. The description paragraph explains what Ground Level is
  3. A "Subscribe on Substack" button is visible and prominent
  4. Clicking the button opens Substack in a new tab
  5. Below the CTA, 6 recent posts are displayed with titles, dates, and excerpts
  6. Post links open the actual Substack articles

  **Check for both pages:**
  - Text is readable (not stretched too wide, good contrast)
  - Layout breathes (generous whitespace between sections)
  - Mobile: resize the browser window narrow and verify sections stack properly
  - No remnants of old homepage structure visible

  Resume signal: Type "approved" if both pages look good, or describe what needs to change.
  </action>
  <verify>User visually confirms both pages match the "person first" brief and all content renders correctly</verify>
  <done>User has approved the visual appearance of both the homepage and Ground Level page</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -5
```
Build must complete with zero errors. The dist/ground-level/index.html output should contain:
- "Ground Level" in an h1
- The description paragraph about cannabis, public health, and policy
- A .btn link with "Subscribe on Substack"
- SubstackFeed rendered content (6 posts or fallback link)
</verification>

<success_criteria>
1. `npm run build` succeeds
2. Ground Level page has the locked description paragraph
3. Ground Level page has a "Subscribe on Substack" button linking to SUBSTACK_URL
4. Ground Level page shows 6 Substack posts (or graceful fallback)
5. Both homepage and Ground Level pass visual inspection by the user
6. No remnants of old page structures remain on either page
</success_criteria>

<output>
After completion, create `.planning/phases/04-rss-dependent-pages/04-02-SUMMARY.md`
</output>
