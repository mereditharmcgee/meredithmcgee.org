---
phase: 05-design-system-elevation
plan: 03
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/work.astro
  - src/pages/work/archive.astro
  - src/pages/contact.astro
  - src/pages/ground-level.astro
autonomous: true
requirements:
  - DSGN-03
  - DSGN-05

must_haves:
  truths:
    - "Every section on every page has generous breathing room — no content section uses less than 4rem vertical padding on desktop"
    - "Scrolling down any page triggers subtle fade-rise entrance animations on key sections via animate-on-scroll marker classes"
    - "About page essay body paragraphs do NOT have scroll-triggered animations — only the heading section animates"
    - "Elements above the fold on page load animate immediately or start visible — no flash of invisible content"
    - "The site renders correctly on a phone screen — tap targets are adequate (minimum 44px touch dimension), text is readable without zooming, mobile nav works"
    - "All existing voice-rule-compliant copy is preserved unchanged — no em dashes, no corporate buzzwords introduced"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Homepage with animation classes on sections and improved spacing"
      min_lines: 100
      contains: "animate-on-scroll"
    - path: "src/pages/about.astro"
      provides: "About page with animate-on-scroll on heading only"
      min_lines: 100
      contains: "animate-on-scroll"
    - path: "src/pages/work.astro"
      provides: "Work page with animation classes and improved spacing"
      min_lines: 80
      contains: "animate-on-scroll"
    - path: "src/pages/work/archive.astro"
      provides: "Archive page with animation classes and improved spacing"
      min_lines: 80
      contains: "animate-on-scroll"
    - path: "src/pages/contact.astro"
      provides: "Contact page with animation classes and improved spacing"
      min_lines: 150
      contains: "animate-on-scroll"
    - path: "src/pages/ground-level.astro"
      provides: "Ground Level page with animation classes and improved spacing"
      min_lines: 70
      contains: "animate-on-scroll"
  key_links:
    - from: "src/styles/global.css"
      to: "all pages"
      via: "Animation CSS classes (.animate-on-scroll converted to .will-animate by observer from 05-02)"
      pattern: "animate-on-scroll"
---

<objective>
Apply animation marker classes and spacing improvements to all six page files. Each page gets `animate-on-scroll` classes on the right elements and increased section padding for generous breathing room.

Purpose: This plan applies the animation infrastructure from 05-02 to each page. The observer script in BaseHead (from 05-02) converts `.animate-on-scroll` to `.will-animate` at runtime, so adding these marker classes is what makes the animations actually appear. Spacing is also improved per-page to ensure every section feels generous.

Output: All six page files updated with animation classes and spacing improvements.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-system-elevation/05-CONTEXT.md
@.planning/phases/05-design-system-elevation/05-RESEARCH.md
@.planning/phases/05-design-system-elevation/05-01-SUMMARY.md
@.planning/phases/05-design-system-elevation/05-02-SUMMARY.md

Source files to read before implementing:
@src/pages/index.astro
@src/pages/about.astro
@src/pages/work.astro
@src/pages/work/archive.astro
@src/pages/contact.astro
@src/pages/ground-level.astro

<interfaces>
<!-- Animation targets per page (from RESEARCH.md) -->

Animation targets per page:
- Homepage: h1+hero-photo+intro-copy as unit; h2+"working on" section heading; h2+"selected work" heading
- About: heading section ONLY (hero). Do NOT animate essay body paragraphs.
- Work: hero section
- Archive: hero section
- Ground Level: intro section (h1+description+CTA)
- Contact: hero section; contact content block

Note: SubstackFeed and WorkCard items CANNOT have animation classes added from the parent page because they are rendered by their own components. Component-level stagger animation is addressed in 05-04. For now, animate section headings and top-level containers.

**Current section padding values (to improve):**
- global.css default: `section { padding: 5rem 0; }` (mobile: 3rem 0)
- index.astro .intro: 6rem top, 4rem bottom
- index.astro .working-on: 4rem 0
- index.astro .selected-work: 4rem 0
- about.astro .about-heading: 5rem 0 3rem (tight bottom)
- about.astro .about-essay: 4rem 0
- work.astro .work-hero: 5rem top, 2.5rem bottom (tight)
- work.astro .work-list: 2.5rem 0 3rem (TIGHT — needs more top padding)
- archive.astro: same tight pattern as work.astro
- contact.astro .contact-hero: 5rem top, 2.5rem bottom
- contact.astro .contact-content: 3rem 0 4rem
- ground-level.astro .gl-intro: 6rem top, 3rem bottom
- ground-level.astro .gl-posts: 3rem 0 4rem
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add animation classes and fix spacing on index.astro (Homepage)</name>
  <files>src/pages/index.astro</files>
  <action>
  Edit src/pages/index.astro to add animation target classes and improve spacing.

  **Animation classes to add:**

  1. The entire intro content (h1, hero-photo, intro-copy) inside .intro .container — wrap or add `class="animate-on-scroll"` to the container div:
     ```html
     <div class="container animate-on-scroll">
     ```

  2. The .working-on section heading:
     ```html
     <h2 class="animate-on-scroll">What I'm working on</h2>
     ```

  3. The .selected-work section heading:
     ```html
     <h2 class="animate-on-scroll">Selected work</h2>
     ```

  Note: SubstackFeed and WorkCard items can NOT have animation classes added from the parent page because they are rendered by their own components. The component-level stagger animation will be addressed in 05-04. For now, animate section headings and top-level containers.

  **Spacing improvements in the scoped <style> block:**

  - `.working-on` padding: change from `4rem 0` to `5rem 0` for more breathing room between beats
  - `.selected-work` padding: change from `4rem 0` to `5rem 0`
  - Keep `.intro` padding as-is (6rem top is correct for below fixed header)

  **Anti-patterns:**
  - Do NOT change the intro copy text — it is FINAL and verbatim
  - Do NOT change component props (SubstackFeed limit, WorkCard props)
  - Do NOT add animation classes to individual WorkCard or SubstackFeed items from this file
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Build succeeds
  - index.astro has animate-on-scroll classes on the intro container and section headings
  - .working-on and .selected-work sections have 5rem 0 padding
  - Intro copy is unchanged verbatim
  </done>
</task>

<task type="auto">
  <name>Task 2: Add animation classes and fix spacing on about.astro</name>
  <files>src/pages/about.astro</files>
  <action>
  Edit src/pages/about.astro.

  **Animation classes:**
  - Add `animate-on-scroll` to the .about-heading container div:
    ```html
    <div class="container animate-on-scroll">
    ```
  - Do NOT add animation classes to the essay body or individual paragraphs — this is a continuous reading experience and animating paragraphs would be jarring (noted in RESEARCH.md pitfall #2)

  **Spacing improvements in scoped <style>:**
  - `.about-heading` padding: change from `5rem 0 3rem` to `5rem 0 3.5rem` (slightly more bottom)
  - `.about-essay` padding: change from `4rem 0` to `4.5rem 0 5rem` (more bottom breathing room)

  **Anti-patterns:**
  - Do NOT animate essay paragraphs
  - Do NOT change the essay text content
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Build succeeds
  - about.astro .about-heading container has animate-on-scroll
  - No animate-on-scroll classes appear inside the .about-essay section
  - Spacing values are updated
  </done>
</task>

<task type="auto">
  <name>Task 3: Add animation classes and fix spacing on work.astro</name>
  <files>src/pages/work.astro</files>
  <action>
  Edit src/pages/work.astro.

  **Animation classes:**
  - Add `animate-on-scroll` to the .work-hero container div

  **Spacing improvements in scoped <style>:**
  - `.work-hero` padding: change from `padding-top: 5rem; padding-bottom: 2.5rem` to `padding-top: 5rem; padding-bottom: 3.5rem`
  - `.work-list` padding: change from `2.5rem 0 3rem` to `4rem 0 5rem` (this was identified as tight in the research)

  **Anti-patterns:**
  - Do NOT change WorkCard component usage or props
  - Do NOT change the hero intro text
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Build succeeds
  - work.astro has animate-on-scroll on the .work-hero container div
  - .work-hero has padding-bottom: 3.5rem
  - .work-list has padding: 4rem 0 5rem
  - WorkCard props and hero intro text are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 4: Add animation classes and fix spacing on work/archive.astro</name>
  <files>src/pages/work/archive.astro</files>
  <action>
  Edit src/pages/work/archive.astro.

  **Animation classes:**
  - Add `animate-on-scroll` to the .archive-hero container div

  **Spacing improvements in scoped <style>:**
  - `.archive-hero` padding: change from `padding-top: 5rem; padding-bottom: 2.5rem` to `padding-top: 5rem; padding-bottom: 3.5rem`
  - `.archive-list` padding: change from `2.5rem 0 3rem` to `4rem 0 5rem`

  **Anti-patterns:**
  - Do NOT change WorkCard component usage or props
  - Do NOT change the archive intro text
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Build succeeds
  - archive.astro has animate-on-scroll on the .archive-hero container div
  - .archive-hero has padding-bottom: 3.5rem
  - .archive-list has padding: 4rem 0 5rem
  - WorkCard props and archive intro text are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 5: Add animation classes and fix spacing on contact.astro</name>
  <files>src/pages/contact.astro</files>
  <action>
  Edit src/pages/contact.astro.

  **Animation classes:**
  - Add `animate-on-scroll` to the .contact-hero container div
  - Add `animate-on-scroll` to the .contact-content container div

  **Spacing improvements in scoped <style>:**
  - `.contact-hero` padding: change from `padding-top: 5rem; padding-bottom: 2.5rem` to `padding-top: 5rem; padding-bottom: 3.5rem`
  - `.contact-content` padding: change from `3rem 0 4rem` to `4rem 0 5rem`

  **Mobile tap target check:**
  - The `.btn.submit-btn` button inherits `.btn` padding of `0.75rem 2rem`. At font-size 1rem, this produces a height of approximately 48px — meets the 44px minimum. No change needed.
  - Form inputs have `padding: 0.75rem` which gives adequate touch height. No change needed.

  **Anti-patterns:**
  - Do NOT change the form action URL or form fields
  - Do NOT change the contact copy text
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Build succeeds
  - contact.astro has animate-on-scroll on both hero and content containers
  - Spacing values are updated
  </done>
</task>

<task type="auto">
  <name>Task 6: Add animation classes and fix spacing on ground-level.astro</name>
  <files>src/pages/ground-level.astro</files>
  <action>
  Edit src/pages/ground-level.astro.

  **Animation classes:**
  - Add `animate-on-scroll` to the .gl-intro container div

  **Spacing improvements in scoped <style>:**
  - `.gl-posts` padding: change from `3rem 0 4rem` to `4rem 0 5rem` (more breathing room for the post feed)

  **Anti-patterns:**
  - Do NOT change the description copy or CTA markup
  - Do NOT change SubstackFeed props
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Build succeeds
  - ground-level.astro has animate-on-scroll on the intro container
  - .gl-posts has updated padding (4rem 0 5rem)
  </done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -5
```
Build must complete with zero errors. After the build:
1. All 6 page files have animate-on-scroll classes on the correct elements
2. About page has NO animation classes in the essay body
3. All section padding values meet the 4rem minimum on desktop
4. No page copy has been changed
</verification>

<success_criteria>
1. `npm run build` succeeds with zero errors
2. All 6 pages have animate-on-scroll marker classes on the right elements
3. About page essay body has no animation classes
4. All sections have at least 4rem vertical padding on desktop
5. All page copy is unchanged
6. Site renders correctly on mobile viewport
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-elevation/05-03-SUMMARY.md`
</output>
