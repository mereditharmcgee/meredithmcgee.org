---
phase: 05-design-system-elevation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/consts.ts
  - src/components/BaseHead.astro
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-07

must_haves:
  truths:
    - "The :root color tokens in global.css are updated to warmer, richer values — deeper forest green for --color-dark, sage for --color-medium, warmer gold and cream tones — while remaining recognizably in the same green/warm family"
    - "The dead Three Audiences tokens (--color-journalist, --color-staffer, --color-researcher, --highlight-transition) are removed from global.css"
    - "Heading typography uses letter-spacing: -0.02em on h1/h2 for an editorial feel, and body line-height is increased to 1.75 for better dark-background readability"
    - "SITE_TITLE in consts.ts is 'Meredith McGee' with no 'MPH'"
    - "The Atkinson font preload links are removed from BaseHead.astro"
    - "The .bg-pattern SVG data URI fill color is updated to match the new --color-medium hex value"
    - "All existing voice-rule-compliant copy is preserved unchanged — no em dashes, no corporate buzzwords introduced"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Evolved color tokens, refined typography scale, cleaned-up dead variables"
      min_lines: 150
      contains: "--color-dark"
    - path: "src/consts.ts"
      provides: "Updated SITE_TITLE without MPH"
      min_lines: 3
      contains: "Meredith McGee"
    - path: "src/components/BaseHead.astro"
      provides: "Cleaned BaseHead without Atkinson font preloads"
      min_lines: 30
      contains: "canonicalURL"
  key_links:
    - from: "src/styles/global.css"
      to: "all pages and components"
      via: ":root CSS custom properties cascade to every element"
      pattern: "--color-dark"
    - from: "src/consts.ts"
      to: "src/components/BaseHead.astro"
      via: "SITE_TITLE used in page title meta tags"
      pattern: "SITE_TITLE"
---

<objective>
Evolve the visual identity's foundation: update color tokens to warmer, richer tones, refine the typography scale for Playfair Display headings and Source Sans 3 body text, clean up dead CSS variables from the Three Audiences era, remove unused Atkinson font preloads from BaseHead, and update SITE_TITLE to remove "MPH."

Purpose: This is the keystone change for Phase 5. Every page and component inherits from the :root CSS custom properties in global.css. Updating these tokens first means every subsequent plan (spacing, animations, SEO) works with the final palette and type scale. The palette evolution should feel like a natural maturation of the existing site — warmer and richer, not different.

Output: Updated `src/styles/global.css`, `src/consts.ts`, and `src/components/BaseHead.astro`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-system-elevation/05-CONTEXT.md
@.planning/phases/05-design-system-elevation/05-RESEARCH.md

Source files to read before implementing:
@src/styles/global.css (PRIMARY — all token changes happen here)
@src/consts.ts (SITE_TITLE update)
@src/components/BaseHead.astro (remove Atkinson preloads)

<interfaces>
<!-- Current state of tokens and files that the executor must understand -->

Current :root tokens in global.css (lines 3-23):
```css
:root {
  --color-dark: #3d4a2a;           /* CHANGE: deeper, richer forest green */
  --color-medium: #6b7c4c;         /* CHANGE: warmer sage green */
  --color-light: #a8b18f;          /* CHANGE: warmer muted sage */
  --color-gold: #d4c98e;           /* CHANGE: warmer amber-gold */
  --color-cream: #e8e4d0;          /* CHANGE: slightly warmer cream */
  --color-cream-light: #f5f3eb;    /* CHANGE: warm highlight cream */
  --color-white: #ffffff;           /* KEEP */
  --color-dark-overlay: rgba(61, 74, 42, 0.95); /* CHANGE: match new --color-dark */

  /* DEAD — DELETE THESE 4 LINES */
  --color-journalist: #d4c98e;
  --color-staffer: #7fb3b3;
  --color-researcher: #b39ddb;
  --highlight-transition: 350ms ease;
}
```

Current typography in global.css:
```css
body { font-size: 18px; line-height: 1.7; }  /* CHANGE: line-height to 1.75 */
h1, h2, h3, h4, h5, h6 { font-weight: 500; line-height: 1.2; }
h1 { font-size: clamp(2.5rem, 5vw, 4rem); }  /* KEEP (pages override with smaller) */
h2 { font-size: clamp(2rem, 4vw, 3rem); }     /* KEEP */
```

Current .bg-pattern SVG data URI (line 134):
- Fill color hardcoded as `%236b7c4c` (URL-encoded #6b7c4c)
- Must be updated to match the new --color-medium hex value
- CSS variables CANNOT be used inside SVG data URIs

BaseHead.astro Atkinson preloads to remove (lines 25-26):
```html
<link rel="preload" href="/fonts/atkinson-regular.woff" as="font" type="font/woff" crossorigin />
<link rel="preload" href="/fonts/atkinson-bold.woff" as="font" type="font/woff" crossorigin />
```

consts.ts current SITE_TITLE:
```typescript
export const SITE_TITLE = 'Meredith McGee, MPH';  // CHANGE to 'Meredith McGee'
```

Pages that use bg-light (will be affected by --color-light change):
- about.astro: .about-essay section (essay body bg)
- contact.astro: .contact-content section (form and content bg)

Both override heading/text colors for dark-on-light with .bg-light rules in global.css.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolve color tokens and clean dead variables in global.css</name>
  <files>src/styles/global.css</files>
  <action>
  Edit src/styles/global.css :root block. Make the following changes:

  **1. Replace the color palette tokens with evolved values.**

  Design direction from CONTEXT.md: "move the current olive greens toward sage, forest, and moss tones with warmer golds and tans. Less military olive, more natural warmth."

  New token values — choose hex values that match this direction. The values below are guidance; use your judgment to find the right balance between "recognizably the same site" and "elevated":

  ```css
  :root {
    /* Evolved palette: warmer, deeper, more organic */
    --color-dark: #2d3a1e;            /* Deeper forest green — richer than the old gray-olive */
    --color-medium: #5a6e3c;          /* Warmer sage — less gray, more green */
    --color-light: #9baa7c;           /* Muted sage — warmer undertone */

    --color-gold: #d4b968;            /* Warmer amber-gold — less washed, more warmth */
    --color-cream: #ece6d0;           /* Warm parchment cream */
    --color-cream-light: #f7f4ec;     /* Warm highlight */

    --color-white: #ffffff;
    --color-dark-overlay: rgba(45, 58, 30, 0.95); /* Match new --color-dark RGB values */

    /* ADD: muted text token for dates, metadata, secondary text */
    --color-text-muted: rgba(236, 230, 208, 0.6); /* cream at 60% opacity */
  }
  ```

  **IMPORTANT:** The `--color-dark-overlay` rgba values MUST match the new `--color-dark` hex value. Convert the hex to RGB for the rgba() function.

  **2. Delete the dead Three Audiences tokens.** Remove these 4 lines entirely:
  ```css
  --color-journalist: #d4c98e;
  --color-staffer: #7fb3b3;
  --color-researcher: #b39ddb;
  --highlight-transition: 350ms ease;
  ```

  **3. Update the .bg-pattern SVG data URI fill color.** The current SVG in the `background-image` url() uses `%236b7c4c` (the old --color-medium). Replace it with the URL-encoded version of the new --color-medium hex value.

  To URL-encode a hex color for use in an SVG data URI: replace `#` with `%23`. Example: `#5a6e3c` becomes `%235a6e3c`.

  Consider also adjusting the fill-opacity from 0.05 to 0.07 or 0.08 so the pattern is faintly visible as a texture rather than invisible.

  **Anti-patterns:**
  - Do NOT change any CSS property names, class names, or selector structure
  - Do NOT use CSS custom properties inside the SVG data URI (they don't work there)
  - Do NOT change the .bg-light override rules — just let them use the updated token values
  - Do NOT change the Google Fonts @import line — both fonts and all weights are needed
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - Build succeeds with zero errors
  - :root block has no Three Audiences tokens (no --color-journalist, --color-staffer, --color-researcher, --highlight-transition)
  - :root block has evolved color values that are recognizably green/warm but richer
  - A new --color-text-muted token exists
  - --color-dark-overlay rgba values match the new --color-dark hex
  - .bg-pattern SVG data URI fill color matches the new --color-medium value
  </done>
</task>

<task type="auto">
  <name>Task 2: Refine typography scale in global.css</name>
  <files>src/styles/global.css</files>
  <action>
  Edit the typography rules in src/styles/global.css. These are global rules that affect every page.

  **1. Add letter-spacing to headings for editorial feel:**
  ```css
  h1, h2, h3, h4, h5, h6 {
    font-family: 'Playfair Display', Georgia, serif;
    color: var(--color-gold);
    line-height: 1.2;
    font-weight: 500;
    letter-spacing: -0.02em;  /* ADD: tighter tracking for editorial feel */
  }
  ```

  **2. Increase body line-height for dark-background readability:**
  ```css
  body {
    font-family: 'Source Sans 3', -apple-system, BlinkMacSystemFont, sans-serif;
    background-color: var(--color-dark);
    color: var(--color-cream);
    font-size: 18px;
    line-height: 1.75;          /* CHANGE: from 1.7 to 1.75 */
    overflow-x: hidden;
    font-optical-sizing: auto;  /* ADD: enable optical sizing */
  }
  ```

  **3. Keep the clamp() heading scale unchanged.** The h1/h2/h3/h4 font-size rules are fine as-is. Individual pages already override with smaller values via scoped styles.

  **Anti-patterns:**
  - Do NOT change heading font-weight from 500 — this is the right Playfair Display weight
  - Do NOT change the body font-size from 18px — pages are already designed around this
  - Do NOT touch the mobile breakpoint body font-size (16px) — it's correct
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Build succeeds
  - h1-h6 rule includes letter-spacing: -0.02em
  - body rule has line-height: 1.75 and font-optical-sizing: auto
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove Atkinson font preloads from BaseHead.astro</name>
  <files>src/components/BaseHead.astro</files>
  <action>
  Edit src/components/BaseHead.astro to remove the two Atkinson font preload lines:

  DELETE these two lines:
  ```html
  <link rel="preload" href="/fonts/atkinson-regular.woff" as="font" type="font/woff" crossorigin />
  <link rel="preload" href="/fonts/atkinson-bold.woff" as="font" type="font/woff" crossorigin />
  ```

  Also delete the `<!-- Font preloads -->` comment line above them, since there will be no font preloads remaining.

  Reason: The Atkinson font files exist in public/fonts/ but NO CSS anywhere references them. The site uses Playfair Display + Source Sans 3 from Google Fonts CDN. These preload tags cause wasted network requests on every page load.

  Do NOT change anything else in BaseHead.astro. The OG image default will be updated in Plan 05-03.
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Build succeeds
  - BaseHead.astro contains no references to "atkinson" (case-insensitive)
  - BaseHead.astro still has all meta tags, canonical URL, OG tags intact
  </done>
</task>

<task type="auto">
  <name>Task 4: Update SITE_TITLE in consts.ts</name>
  <files>src/consts.ts</files>
  <action>
  Edit src/consts.ts to change SITE_TITLE:

  FROM: `export const SITE_TITLE = 'Meredith McGee, MPH';`
  TO:   `export const SITE_TITLE = 'Meredith McGee';`

  Do NOT change SITE_DESCRIPTION, SUBSTACK_FEED_URL, or SUBSTACK_URL.

  Reason: User decision in CONTEXT.md — "Site title: 'Meredith McGee' — drop the 'MPH' from SITE_TITLE in consts.ts. Matches the homepage h1."
  </action>
  <verify>
    <automated>cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
  - Build succeeds
  - SITE_TITLE is exactly 'Meredith McGee' (no MPH, no comma)
  - All other exports in consts.ts are unchanged
  </done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/mmcge/my-website && npm run build 2>&1 | tail -5
```
Build must complete with zero errors. After the build:
1. global.css :root has no Three Audiences tokens
2. global.css has evolved color values (deeper forest green, warmer gold, new muted text token)
3. global.css headings have letter-spacing: -0.02em
4. global.css body has line-height: 1.75 and font-optical-sizing: auto
5. global.css .bg-pattern SVG fill matches new --color-medium
6. BaseHead.astro has no Atkinson font preloads
7. consts.ts SITE_TITLE is 'Meredith McGee'
</verification>

<success_criteria>
1. `npm run build` succeeds with zero errors
2. The color palette is evolved — warmer, richer, recognizably the same green/warm family
3. Dead Three Audiences tokens are gone from the codebase
4. Typography has editorial letter-spacing and improved body line-height
5. Atkinson font preloads removed from BaseHead
6. SITE_TITLE no longer contains "MPH"
7. All six pages render without broken styles or missing colors
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-system-elevation/05-01-SUMMARY.md`
</output>
