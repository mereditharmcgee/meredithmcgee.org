---
phase: 03-static-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/work.astro
files_created:
  - src/pages/work/archive.astro
autonomous: false
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
  - WORK-05
  - WORK-06

must_haves:
  truths:
    - "The Work page shows curated entries organized by actual pieces of work, not abstract categories or services"
    - "Each work entry displays title, short description, category tag, and link (if available) via the WorkCard component"
    - "Only the 4 featured items from work.ts appear on the main Work page"
    - "Category tags are visible but subtle on each work entry"
    - "A text link at the bottom of the Work page leads to the Work Archive at /work/archive"
    - "The Work Archive page is NOT in the main navigation"
    - "The Work Archive shows all 8 work items (featured + non-featured) as a flat list using WorkCard"
    - "Work page placeholder entries have real descriptive text (from work.ts)"
  artifacts:
    - path: "src/pages/work.astro"
      provides: "Curated Work page with 4 featured items and archive link"
      contains: "WorkCard"
    - path: "src/pages/work/archive.astro"
      provides: "Comprehensive work archive listing all 8 items"
      contains: "WorkCard"
  key_links:
    - from: "src/pages/work.astro"
      to: "src/components/WorkCard.astro"
      via: "import WorkCard"
    - from: "src/pages/work.astro"
      to: "src/data/work.ts"
      via: "import { work }"
    - from: "src/pages/work.astro"
      to: "/work/archive"
      via: "anchor link"
    - from: "src/pages/work/archive.astro"
      to: "src/components/WorkCard.astro"
      via: "import WorkCard"
    - from: "src/pages/work/archive.astro"
      to: "src/data/work.ts"
      via: "import { work }"
---

<objective>
Restructure the Work page from a services/capabilities layout into a curated display of actual work pieces, and create a new Work Archive page showing the complete list.

Purpose: The current Work page reads like a consulting brochure (numbered service blocks, focus cards, interest tags, CTA). The new version lets actual work speak for itself through the WorkCard component, with a separate archive for the full list.

Output: Rewritten `src/pages/work.astro` with 4 featured WorkCards and an archive link, plus new `src/pages/work/archive.astro` with all 8 WorkCards.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-static-pages/03-CONTEXT.md
@.planning/phases/03-static-pages/03-RESEARCH.md
@.planning/phases/02-shared-components/02-02-SUMMARY.md

<interfaces>
<!-- Components and data the executor needs -->

From src/components/WorkCard.astro:
```typescript
interface Props {
  title: string;
  description: string;
  category: string;
  link?: string;
}
```
WorkCard is styled for dark backgrounds (uses var(--color-gold) for titles, var(--color-cream) for descriptions). Place WorkCards inside bg-dark or bg-medium sections.

From src/data/work.ts:
```typescript
export interface WorkItem {
  title: string;
  description: string;
  category: string;
  link?: string;
  featured: boolean;
}

export const work: WorkItem[] = [/* 8 items, 4 featured */];
```

Featured items (featured: true):
1. Cannabis and Psychiatric Risk: A Two-Study Analysis (RESEARCH, has link)
2. Parabola Center Content Strategy (WRITING, has link)
3. Bradbury-Sullivan LGBT Community Center Grant Portfolio (GRANTS, no link)
4. Yale School of Public Health Program Evaluation (EVALUATION, has link)

Non-featured items (featured: false):
5. Ground Level Newsletter (WRITING, has link)
6. Hemp-Derived THC Policy Analysis (POLICY, no link)
7. LGBTQ+ Health Equity Research (RESEARCH, no link)
8. Community Health Needs Assessment (EVALUATION, no link)

From src/components/BaseHead.astro:
```typescript
interface Props {
  title: string;
  description: string;
  image?: string;
}
```

From src/consts.ts:
```typescript
export const SITE_TITLE = 'Meredith McGee, MPH';
```

Page template pattern:
```astro
---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE } from '../consts';
---
<!doctype html>
<html lang="en">
  <head><BaseHead title={...} description="..." /></head>
  <body>
    <Header />
    <!-- content -->
    <Footer />
  </body>
</html>
```

Note for archive page: imports are two levels up from src/pages/work/:
```
import WorkCard from '../../components/WorkCard.astro';
import { work } from '../../data/work';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite work.astro with featured WorkCards and archive link</name>
  <files>src/pages/work.astro</files>
  <action>
Fully rewrite `src/pages/work.astro`. Delete ALL current content (service blocks, focus cards, interest tags, CTA section).

**New structure:**

1. **Hero/intro section** (bg-dark bg-pattern):
   - `<h1>Work</h1>` (not "How I Work" or "My Work")
   - 1-2 short paragraphs introducing what kind of work Meredith does. Keep it brief and concrete. Example tone: "I work at the intersection of public health, writing, and research. Here's some of what that looks like in practice." Use contractions. No em dashes. No corporate buzzwords.
   - Max-width: 800px container, centered text

2. **Featured work section** (bg-dark):
   - Filter `work` array to `featured === true` items
   - Render each as a `<WorkCard title={item.title} description={item.description} category={item.category} link={item.link} />`
   - Wrap WorkCards in a container with max-width: 800px
   - WorkCard is designed for dark backgrounds, so bg-dark is correct here

3. **Archive link** (same section or new bg-medium section):
   - Simple text link: `<a href="/work/archive">See all work &rarr;</a>`
   - Style it understated: not a button, not a CTA. Just a simple link in gold or cream color with subtle hover.
   - Center it below the last WorkCard

**Imports needed:**
```
import WorkCard from '../components/WorkCard.astro';
import { work } from '../data/work';
```

**Voice rules for intro copy:**
- Contractions always
- No em dashes
- No corporate language ("leverage", "stakeholder", "cross-disciplinary")
- Lead with concrete, not thesis
- One sentence about interests (public health, writing, research) woven naturally, not as a list

**CSS: Scoped `<style>` block.** Use global utilities (.container, .bg-dark, .bg-pattern, .bg-medium) from global.css. Do not duplicate them.
  </action>
  <verify>
    <automated>cd "C:/Users/mmcge/my-website" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- work.astro contains zero service-block elements, zero focus-card elements, zero interest-tag elements, zero numbered service blocks
- work.astro imports WorkCard and work data
- Exactly 4 WorkCard components render (the featured items)
- A plain text link to /work/archive exists at the bottom
- Intro copy uses contractions and contains no corporate language
  </done>
</task>

<task type="auto">
  <name>Task 2: Create work/archive.astro with all work items</name>
  <files>src/pages/work/archive.astro</files>
  <action>
Create the new file `src/pages/work/archive.astro`. This requires creating the `src/pages/work/` directory (Astro file-based routing will serve it at `/work/archive`).

**Structure:**

1. **Page head:** Same pattern as other pages. Title: `Work Archive | ${SITE_TITLE}`. Description: brief text about comprehensive work listing.

2. **Header section** (bg-dark bg-pattern):
   - `<h1>Work Archive</h1>`
   - Optional: one brief sentence ("A complete list of selected projects and publications.") or skip intro text entirely (Claude's discretion per CONTEXT.md).
   - Max-width: 800px container

3. **Work list section** (bg-dark):
   - Import ALL items from `work` array (no filtering)
   - Render each as `<WorkCard title={item.title} description={item.description} category={item.category} link={item.link} />`
   - Flat list, no grouping by category
   - Wrap in container with max-width: 800px

4. **Back link:**
   - `<a href="/work">&larr; Back to Work</a>` at the bottom for easy navigation back
   - Understated styling matching the archive link on the Work page

**Imports (note: two levels up from src/pages/work/):**
```
import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import Header from '../../components/Header.astro';
import WorkCard from '../../components/WorkCard.astro';
import { SITE_TITLE } from '../../consts';
import { work } from '../../data/work';
```

**CSS: Scoped `<style>` block.** Minimal page-specific styles. The WorkCard component handles its own styling. Mainly need container sizing, link styling, and spacing.

**IMPORTANT:** This page is NOT in the main navigation. It's only reachable from the Work page link. Do NOT modify Header.astro.
  </action>
  <verify>
    <automated>cd "C:/Users/mmcge/my-website" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- src/pages/work/archive.astro exists
- The page renders at /work/archive in the built site
- All 8 work items from work.ts appear on the page
- A back link to /work exists
- The page is NOT in the main nav (Header.astro unchanged)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Work page and Archive</name>
  <files>src/pages/work.astro, src/pages/work/archive.astro</files>
  <action>
Human verification of Work page restructure and new Work Archive page.

Steps to verify:
1. Run `npm run dev` from the project root
2. Visit http://localhost:4321/work
3. Verify: 4 work items display with title, description, and category tags
4. Verify: Items with links show clickable titles and "Read more" arrows
5. Verify: No service blocks, focus cards, or interest tags remain
6. Verify: A "See all work" link appears at the bottom
7. Click the archive link to navigate to /work/archive
8. Verify: All 8 work items appear
9. Verify: A "Back to Work" link exists
10. Check main nav: "Work Archive" should NOT appear in navigation
11. On mobile (resize to ~375px): cards stack properly, text readable

Type "approved" or describe specific issues to fix.
  </action>
  <verify>
    <automated>cd "C:/Users/mmcge/my-website" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually confirmed the Work page shows 4 featured items and the Archive page shows all 8 items with correct navigation between them.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with exit code 0
- /work renders in browser with 4 featured WorkCards
- /work/archive renders in browser with all 8 WorkCards
- Clicking "See all work" on /work navigates to /work/archive
- /work/archive is not in the site navigation
</verification>

<success_criteria>
1. Work page shows curated entries organized by actual pieces of work (WORK-01)
2. Each entry shows title, description, link, and category tag via WorkCard (WORK-02, WORK-04)
3. Work page uses placeholder entries with real descriptive text from work.ts (WORK-03)
4. Work Archive is linked from Work page bottom, not in main nav (WORK-05)
5. Work Archive is a comprehensive listing of all items (WORK-06)
</success_criteria>

<output>
After completion, create `.planning/phases/03-static-pages/03-02-SUMMARY.md`
</output>
