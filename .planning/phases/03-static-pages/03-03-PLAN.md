---
phase: 03-static-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/contact.astro
autonomous: false
requirements:
  - CNTC-01
  - CNTC-02
  - CNTC-03

must_haves:
  truths:
    - "The Contact page is warm, human, and direct with the email address displayed plainly"
    - "LinkedIn and Substack links are present on the Contact page"
    - "All copy uses contractions, contains no em dashes, no bullet lists, and no corporate language"
    - "The form has exactly 3 fields: name, email, message (subject dropdown removed)"
    - "The page uses a single centered column layout, not a two-column grid"
    - "The tone reads like a coffee shop conversation, not a corporate contact form"
  artifacts:
    - path: "src/pages/contact.astro"
      provides: "Warm, simplified Contact page with centered layout and 3-field form"
      contains: "formspree"
  key_links:
    - from: "src/pages/contact.astro"
      to: "https://formspree.io/f/xgooeagz"
      via: "form action attribute"
    - from: "src/pages/contact.astro"
      to: "src/components/Header.astro"
      via: "import Header"
    - from: "src/pages/contact.astro"
      to: "src/components/Footer.astro"
      via: "import Footer"
---

<objective>
Simplify the Contact page from a two-column corporate layout with interest bullets and subject dropdown into a warm, single-column page with casual voice, plain email, social links, and a 3-field form.

Purpose: The current Contact page reads corporate ("I welcome the conversation", bulleted interest list, subject dropdown). The new version should feel like talking to someone at a coffee shop.

Output: Rewritten `src/pages/contact.astro` with centered single-column layout, casual voice, and simplified form.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-static-pages/03-CONTEXT.md
@.planning/phases/03-static-pages/03-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs -->

From src/components/BaseHead.astro:
```typescript
interface Props {
  title: string;
  description: string;
  image?: string;
}
```

From src/consts.ts:
```typescript
export const SITE_TITLE = 'Meredith McGee, MPH';
export const SUBSTACK_URL = 'https://meredithmcgee.substack.com';
```

Contact details (confirmed in RESEARCH.md):
- Email: meredith.ar.mcgee@gmail.com
- LinkedIn: https://www.linkedin.com/in/meredith-mcgee
- Substack: https://meredithmcgee.substack.com
- Formspree endpoint: https://formspree.io/f/xgooeagz

Current form fields to KEEP: name, email, message
Current form fields to REMOVE: subject dropdown (entire form-group block)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite contact.astro with centered layout and casual voice</name>
  <files>src/pages/contact.astro</files>
  <action>
Fully rewrite `src/pages/contact.astro`. Replace the current two-column grid layout with a single centered column.

**New structure (single centered column, max-width: 700px):**

1. **Header section** (bg-dark bg-pattern):
   - `<h1>Get in Touch</h1>` (or "Say Hello" or similar casual heading)
   - No intro paragraph in the hero. Keep it clean.

2. **Content section** (bg-light):
   - Single centered column container, max-width: 700px
   - **Warm intro text (1-2 short paragraphs):**
     Write genuinely casual copy. Target tone: coffee shop conversation. Example direction (adapt, don't copy verbatim):
     "I'd love to hear from you. Whether it's about public health, writing, community work, or something I haven't thought of yet, I'm always up for a good conversation."

     Voice rules (MUST follow all):
     - Contractions always ("I'd", "it's", "I'm", "don't", "hasn't", "you're")
     - No em dashes (no \u2014 character)
     - No bullet lists
     - No corporate language ("I welcome the conversation", "leverage", "stakeholder", "collaborate on synergies")
     - One casual sentence mentioning interests (public health, writing, community work) woven naturally into the text, NOT as a bulleted list
     - Vary sentence length
     - Lead with concrete

   - **Email displayed plainly:**
     `<a href="mailto:meredith.ar.mcgee@gmail.com">meredith.ar.mcgee@gmail.com</a>`
     Style it prominently but simply. Not buried in a paragraph.

   - **Social links:**
     LinkedIn and Substack links, displayed as simple text links (not icons, since those are in the footer). Example:
     "You can also find me on <a href="https://www.linkedin.com/in/meredith-mcgee">LinkedIn</a> and <a href="https://meredithmcgee.substack.com">Substack</a>."
     Or as a small list of two links below the email. Claude's discretion on exact presentation.

   - **Simplified form** (below the text, still within the same centered column):
     Keep the dark form box styling (bg-dark card with border-radius). Formspree endpoint stays: `https://formspree.io/f/xgooeagz`.

     Fields (only these three):
     - Name: `<input type="text" id="name" name="name" required />`
     - Email: `<input type="email" id="email" name="email" required />`
     - Message: `<textarea id="message" name="message" rows="5" required placeholder="What's on your mind?"></textarea>`

     **DELETE the subject dropdown entirely** (the entire form-group containing `<select id="subject">`).

     Submit button: "Send Message" (keep existing btn styling)

   - A brief line after the form like "I'll get back to you as soon as I can." or similar casual sign-off. Not required but nice.

**What to DELETE from current contact.astro:**
- The two-column `.contact-grid` layout
- The `.contact-info` left column
- The bulleted `.interest-list` (entirely)
- The "Prefer email?" subheading structure
- The `<select id="subject">` dropdown and its form-group
- The separate `.contact-form-wrapper` right column styling

**CSS: Scoped `<style>` block.** Much simpler than current. Single column, centered. Keep the dark form card styling adapted from the current `.contact-form-wrapper`. Remove all two-column grid CSS, interest list CSS.

Responsive: At 768px, reduce padding. The single column layout is inherently mobile-friendly.
  </action>
  <verify>
    <automated>cd "C:/Users/mmcge/my-website" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
- contact.astro has a single centered column layout (no .contact-grid, no two-column grid)
- The subject dropdown is gone (no select element in the file)
- The interest bullet list is gone (no .interest-list, no ul/li elements)
- Email address meredith.ar.mcgee@gmail.com is displayed as a mailto link
- LinkedIn and Substack links are present
- Form has exactly 3 fields: name, email, message
- Form action points to https://formspree.io/f/xgooeagz
- All copy uses contractions
- No em dashes in the file
- No corporate language ("I welcome the conversation" is gone)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Contact page</name>
  <files>src/pages/contact.astro</files>
  <action>
Human verification of the Contact page simplification.

Steps to verify:
1. Run `npm run dev` from the project root
2. Visit http://localhost:4321/contact
3. Verify: Page uses a single centered column (not two columns side by side)
4. Verify: Tone is warm and casual (read the intro text)
5. Verify: Email address is visible and clickable
6. Verify: LinkedIn and Substack links are present
7. Verify: Form has only 3 fields (name, email, message). No subject dropdown.
8. Verify: No bulleted interest list visible
9. Verify: Copy uses contractions and has no corporate feel
10. On mobile (resize to ~375px): form is usable, text readable

Type "approved" or describe specific issues to fix.
  </action>
  <verify>
    <automated>cd "C:/Users/mmcge/my-website" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually confirmed the Contact page has a centered layout, casual tone, visible email, social links, and simplified 3-field form.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with exit code 0
- /contact renders in browser with single-column layout
- Form submits without error (Formspree endpoint unchanged)
- No subject dropdown, no interest list, no two-column grid
- Email, LinkedIn, and Substack all present
</verification>

<success_criteria>
1. Contact page is warm, human, and direct with email displayed (CNTC-01)
2. LinkedIn and Substack links are present (CNTC-02)
3. All copy follows voice rules: contractions, no em dashes, no corporate language, no bullet lists (CNTC-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-static-pages/03-03-SUMMARY.md`
</output>
